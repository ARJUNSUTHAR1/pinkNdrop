import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
const SignIn = () => {
    return (
        <SafeAreaView className="flex items-center justify-between text-red-600">
            <Text>Sign -in</Text>
        </SafeAreaView>
    )
}

export default SignIn;